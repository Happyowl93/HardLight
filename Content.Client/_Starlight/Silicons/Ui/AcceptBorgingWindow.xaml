<DefaultWindow xmlns="https://spacestation14.io"
            xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
            Title="{Loc 'accept-borging-window-title'}"
            MinSize="256 256"
            SetSize="256 256">
    <BoxContainer Orientation="Vertical"
                  HorizontalExpand="True"
                  VerticalExpand="True">
        <Label Name="WindowDescription" Visible="False" Text="{Loc 'accept-borging-window-prompt-text-part'}"/>
        <BoxContainer Orientation="Vertical"
                        HorizontalExpand="True"
                        Margin="5 0">
            <Button Name="AcceptButton"
                    Text="{Loc 'accept-borging-window-accept-button'}"
                    Margin="0 0 0 5"
                    Access="Public"/>
            <Button Name="DenyButton"
                    Text="{Loc 'accept-borging-window-deny-button'}"
                    Margin="0 0 0 5"
                    Access="Public"/>
        </BoxContainer>
    </BoxContainer>
</DefaultWindow>
